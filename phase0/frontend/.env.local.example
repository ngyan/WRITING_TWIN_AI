# Backend URL (server-side only — not exposed to browser)
# Local dev:
BACKEND_URL=http://localhost:8000
# Production (set this in Vercel dashboard env vars):
# BACKEND_URL=https://api.writingtwinai.com
